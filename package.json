{
  "name": "JCh-code-tree",
  "displayName": "Code File & Function Tree",
  "description": "Tree view of workspace files (pl/pm/cgi/html/js/css/ts/tsx/sh/json, plus shebang scripts) with Perl/JS/TS/shell functions nested underneath, synced with the cursor.",
  "version": "0.0.21",
  "publisher": "jchakarov",
  "private": true,
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "JChCodeTreeContainer",
          "title": "Code Tree",
          "icon": "media/tree-icon.svg"
        }
      ]
    },
    "views": {
      "JChCodeTreeContainer": [
        {
          "id": "JChCodeTree",
          "name": "Code Tree"
        }
      ]
    },
    "commands": [
      {
        "command": "JChCodeTree.refresh",
        "title": "Code Tree: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "JChCodeTree.openSelectedFile",
        "title": "Code Tree: Open Selected File"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "JChCodeTree.refresh",
          "when": "view == JChCodeTree",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Code Tree",
      "properties": {
        "JChCodeTree.showHiddenFiles": {
          "type": "boolean",
          "default": false,
          "description": "Show files and folders whose name starts with a dot."
        },
        "JChCodeTree.showSymlinks": {
          "type": "boolean",
          "default": false,
          "description": "Show files and folders reached through a symlink (under the link's path). Walks folders directly, independent of search.followSymlinks."
        },
        "JChCodeTree.updateFolder": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "description": "Folder with JCh-code-tree-x.y.z.vsix builds. On startup it is git-pulled (if inside a git clone) and a newer version found there is installed automatically. Empty = disabled."
        }
      }
    },
    "keybindings": [
      {
        "command": "JChCodeTree.openSelectedFile",
        "key": "alt+o",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "bash scripts/package.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^5.4.0"
  }
}
