# Code File & Function Tree

Дърво в Activity Bar-а на VSCode с файловете от workspace-а (по подразбиране `.pl`, `.pm`, `.cgi`, `.html`, `.js`, `.css`, `.ts`, `.tsx`, `.sh`, `.json`, и файлове без разширение с shebang за shell — списъкът се настройва), а под всеки Perl/JS/TS/shell файл — функциите, дефинирани в него.

## Какво прави

- **Дърво**: папки → файлове → функции, вложени едно в друго.
- **Двоен клик върху файл** — отваря файла. Единичен клик само разгъва/свива клона му в дървото.
- **Клик върху функция** — отваря файла с курсора в началото на функцията.
- **Синхронизация с курсора** — докато местиш курсора в отворен файл, функцията, в която се намираш, автоматично се маркира в дървото.
- Дървото се обновява при създаване/изтриване на файлове, а списъкът с функции — докато пишеш (без да е нужен запис).
- **Десен клик** в дървото: Open to the Side (файл/функция), Reveal in Explorer View (файл/папка), Copy Name (функция), Copy Relative Path.

## Outline и търсене на функция (Perl и shell)

- **Outline** панелът, **breadcrumbs** над редактора и `Ctrl+Shift+O` показват функциите в текущия Perl/shell файл.
- `Ctrl+T` търси функция по име в целия проект (Perl и shell).
- За JS/TS това вече го прави вграденият TypeScript на VS Code, затова разширението не ги дублира.
- Функцията се води до реда преди следващата функция (краят ѝ не се разпознава).

## Find All References (`Shift+F12`) — Perl и shell

Върху името на функция показва всички места в проекта, където се среща, само във файлове на същия език.

- Това е текстово търсене на цялата дума, не анализ на кода — брои се и в коментари, стрингове и ключове на хеш със същото име.
- `Foo::bar` се търси като `bar`, така че се намират и `Foo::bar(...)`, и `bar(...)`, и `->bar`, и `&bar`. Променливи (`$bar`, `@bar`, `%bar`) не се броят.
- Работи само ако думата е име на функция, дефинирана някъде в проекта.

## Go to Definition

В Perl/JS/TS/shell файл: `F12` или `Ctrl`+клик върху името на функция скача до дефиницията ѝ.

- Търси се в целия проект, но само във файлове на същия език (извикване от `.pl` намира `sub` в `.pl`/`.pm`/`.cgi`, не функция със същото име в `.js`).
- Разпознават се и Perl имена с пакет (`Foo::Bar::baz`).
- Ако функцията е дефинирана на няколко места, VS Code показва списък с всички.
- Ако под курсора няма позната функция, но има път към файл (напр. `lib/Utils.pm` в `require`/`use`/`import`), се отваря този файл — както при **Open Selected File** по-долу.

## Code Tree: Open Selected File (`Alt+O`)

Отваря файла, чийто път е под курсора — напр. `require "lib/Utils.pm";`, `<script src="js/app.js">`, `<TMPL_INCLUDE NAME=group_permissions.html>`, `source ./env.sh`. Командата е достъпна и от Command Palette.

- Първо пътят се търси спрямо папката на текущия файл.
- Ако там няма такъв файл, се търсят файлове със същото име навсякъде в проекта (сред показаните в дървото).
- При повече от едно съвпадение се показва списък за избор.
- Разпознава се всеки текст от вида `път/до/файл.разширение` (букви, цифри, `_`, `-`, `.`, `/`), без значение от езика на файла.

## Настройки

- `JChCodeTree.extensions` — кои разширения се показват и с кой parser се търсят функциите в тях: `perl`, `js`, `sh`, `none` (показва се без функции) или `hide` (скрива някое от стандартните). Твоите записи се добавят към стандартните, напр.:
  ```json
  "JChCodeTree.extensions": { ".t": "perl", ".psgi": "perl", ".tmpl": "none", ".json": "hide" }
  ```
  Новите Perl/shell разширения получават и Go to Definition, Outline и `Shift+F12`.
- `JChCodeTree.exclude` (по подразбиране `[]`) — glob шаблони спрямо папката на workspace-а за файлове и папки, които да не се показват, напр. `["**/logs", "tmp", "blib/**", "**/*.min.js"]`. Допълнително се спазва и `files.exclude` (каквото е скрито в Explorer-а, е скрито и тук). Изключените файлове не участват и в Go to Definition, `Ctrl+T`, `Shift+F12` и `Alt+O`.
- `JChCodeTree.showHiddenFiles` (по подразбиране `false`) — дали да се показват файлове и папки, чието име започва с `.` (включително папки от multi-root workspace, напр. `.claude`). При промяна дървото се обновява автоматично.
- `JChCodeTree.showSymlinks` (по подразбиране `false`) — дали да се показват файлове и папки, достъпни през symlink (под името на линка). Когато е включена, папките се обхождат директно, независимо от `search.followSymlinks`; защитено е от зацикляне, а `node_modules`/`.git`/`.svn`/`.hg`/`CVS` се пропускат. Тогава е нормално Go to Definition и `Alt+O` да дават дублирани резултати.
- `JChCodeTree.autoUpdate` (по подразбиране `true`) — автоматично обновяване от GitHub (виж по-долу).

## Инсталиране и обновяване

Еднократно: изтегли последния `.vsix` от [version/](https://github.com/JekoCh/JChCodeTree/tree/master/version) и го инсталирай с **Extensions → … → Install from VSIX…**.

След това при всяко отваряне на прозорец разширението сравнява версията си с `package.json` в [github.com/JekoCh/JChCodeTree](https://github.com/JekoCh/JChCodeTree). Ако там има по-нова, тегли `version/JCh-code-tree-x.y.z.vsix`, инсталира го и предлага Reload. Без интернет просто не се обновява до следващото стартиране.

Публикуване на нова версия: `npm run package` (оставя само последния `.vsix` във `version/`; по-старите отиват във `version/old/`, който не се commit-ва), после commit + push.

## Разпознаване на функции

Ползва се regex, не истински parser:

- Perl: `sub name { ... }`
- JS/TS: `function name()`, `const name = function/() => {}`, методи в клас/обект (`name(...) {}`), включително типови анотации (`: Type`, `<T>`) и `public`/`private`/`protected`/`readonly` модификатори
- Shell (`.sh` и файлове без разширение с `#!.../bash` или подобен shebang): `function name { ... }`, `name() { ... }`

Покрива обичайните случаи, но може да пропусне екзотични конструкции (напр. сигнатура на няколко реда).

## Известни ограничения

- `.html`/`.css`/`.json` файловете се показват в дървото, но без вложени функции.
- По подразбиране файлове и папки, достъпни през symlink, не се показват — само реалните пътища (за да няма дублирани резултати). Ако отвориш файл през symlink (напр. от Explorer-а), той се свързва с реалния си файл в дървото и синхронизацията с курсора работи. Не работи, ако реалният файл е извън workspace-а или ако самата workspace папка е symlink.
- Файл без разширение се показва само ако първият му ред е shebang, сочещ към shell (`sh`/`bash`/`zsh`/`ksh`/`dash`).

## Changelog

### 0.0.25
- Нова настройка `JChCodeTree.extensions` — кои разширения се показват и с кой parser (напр. `".t": "perl"`, `".json": "hide"`).
- Нова настройка `JChCodeTree.exclude` — glob шаблони за скриване на файлове и папки; спазва се и `files.exclude`.
- Find All References (`Shift+F12`) за функции в Perl/shell файлове.

### 0.0.24
- Outline, breadcrumbs и `Ctrl+Shift+O` за Perl/shell файлове; `Ctrl+T` търси функция в целия проект.
- Функциите в дървото се обновяват, докато пишеш, а не само при запис.
- Контекстно меню в дървото: Open to the Side, Reveal in Explorer View, Copy Name, Copy Relative Path.
- Поправено: синхронизацията с курсора не работеше за shell скриптове без разширение.
- По-бързо зареждане на дървото: от файловете без разширение се чете само началото (за shebang), не целият файл.

### 0.0.23
- Иконка на разширението.

### 0.0.22
- Автоматично обновяване от GitHub (`JekoCh/JChCodeTree`) — при всяко отваряне на прозорец, ако там има по-нова версия, тя се изтегля, инсталира и се предлага Reload. Без clone и без настройки.
- Нова настройка `JChCodeTree.autoUpdate` (по подразбиране `true`) — с `false` автоматичното обновяване се изключва.

### 0.0.19
- Нова настройка `JChCodeTree.showSymlinks` (по подразбиране `false`) — показва файлове и папки, достъпни през symlink, под името на линка. Папките се обхождат директно, независимо от `search.followSymlinks`, със защита от зацикляне.
- Когато symlink-ите са скрити, файл, отворен през symlink, се свързва с реалния си файл в дървото — синхронизацията с курсора работи, а при запис функциите му се обновяват.
